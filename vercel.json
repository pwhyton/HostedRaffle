{
  "buildCommand": "pnpm --filter @raffle/backend exec prisma generate && pnpm --filter @raffle/frontend build",
  "outputDirectory": "packages/frontend/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/((?!api).*)", "destination": "/index.html" }
  ]
}
