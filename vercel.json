{
  "buildCommand": "pnpm exec prisma generate --schema=packages/backend/prisma/schema.prisma && pnpm --filter @raffle/frontend build",
  "outputDirectory": "packages/frontend/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/((?!api).*)", "destination": "/index.html" }
  ]
}
